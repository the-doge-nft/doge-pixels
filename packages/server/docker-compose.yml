version: "3.8"
services:
  redis:
    image: redis
    container_name: cache
    ports:
      - '6379:6379'

  app:
    build: .
    depends_on:
      - redis
    volumes:
      - .:/app
    ports:
      - '8000:8000'



