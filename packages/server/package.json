{
  "name": "@scaffold-eth/server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv-safe": "^8.2.0",
    "ethers": "^5.5.1",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.15",
    "redis": "^4.0.0",
    "winston": "^3.3.3"
  },
  "scripts": {
    "dev:deploy": "pm2 deploy development",
    "dev": "yarn prestart && pm2 start ecosystem.config.js --watch",
    "dev:start": "yarn prestart && pm2 start ecosystem.config.js --env development",
    "prestart": "node ./scripts/create_contracts.js",
    "stop": "pm2 stop ecosystem.config.js",
    "logs": "pm2 logs",
    "status": "pm2 status",
    "test": "echo $(curl 167.172.252.56/v1/test)"
  }
}
